import { Background, Input } from "../ui";

export default function Settings() {
    
  return (
    <Background>
      <div>
        <Input type="file"></Input>
      </div>
    </Background>
  );
}
